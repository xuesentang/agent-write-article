基于上面确认的技术方案，现在执行项目初始化：特别声明，项目地址为D:\agent-write-article2,开发过程中生成的新文件都放在这个文件夹里

前端初始化建议：
1、使用组件库搭建界面，我之前配置了ui-ux-pro-max 这个Skill，看能不能在这一步用，如果不能就用Ant Design Vue(一个蚂蚁金服出品的企业级UI组件库)
2、在请求封装方面采用Axios配置
3、使用OpenAPI工具，传统方式下，每个后端接口都要在前端手动编写请求函数和 TypeScript 类型定义，不仅繁琐而且容易出错。推荐使用 OpenAPI 工具，它可以读取后端的接口文档，自动生成所有请求函数和类型定义，后端接口一变，重新生成一遍就好。
4、全局布局，我们的网站有统一的顶部导航栏和底部版权信息，只有中间的内容区域会随页面切换而变化。为了避免在每个页面中都重复写导航栏和底部，可以用一个全局布局组件把它们包裹起来，页面内容动态加载。

后端初始化建议：
1、具备必要的基础设施，如错误码枚举、统一响应格式、测试验证
2、整个项目后端按照 Router → Service → Database 三层架构组织：Router 负责接收 HTTP 请求和返回响应，Service 负责业务逻辑，Database 负责数据存取。职责清晰，层次分明。

初始化指导：
1. 创建项目根目录结构（按照方案文档中的目录结构）
2. 初始化前端项目（你确认的框架）
3. 初始化后端项目（你确认的框架）
4. 创建 .env.example 文件，列出所有需要的环境变量
5. 创建 README.md，简要说明项目结构和启动方式
6. 创建.gitignore文件，存入项目中不希望被提交和读取的文件
7、创建名为writearticle-venv的虚拟环境，并安装项目所需的依赖

注意：
- 暂时不实现任何业务逻辑
- 确保 npm install / pip install 后项目能正常启动
- 前端访问后端的代理配置要配好
- 后端需要有一些必要的基础设施，包括错误码枚举、统一响应格式、测试验证

上述内容如果不全面或有不合理之处，请指出，我会及时修改，你需要的信息和权限请告诉我，我会为你提供


# 关闭 Claude Code 后，在管理员 PowerShell 中：
Remove-Item -Path "C:\Users\28707\.claude\plugins\marketplaces\anthropics-claude-plugins-official" -Recurse -Force
